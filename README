# How to Run
- navigate to the base directory (where /backend and /frontend are) and run `docker-compose up --build`
- This creates a mysql-data folder in the same folder (this is the persistent data store)